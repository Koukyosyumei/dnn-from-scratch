[project]
name = "dnn-from-scratch"
version = "0.1.0"
description = "Minimal fully-connected DNN with SGD — h5i demo"
requires-python = ">=3.9"
dependencies = ["numpy"]

[project.optional-dependencies]
dev = ["pytest"]
